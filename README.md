# Свод по опросу владельцев процессов

Публичный одностраничный свод (Слой 1А) — **https://artempv1993-boop.github.io/svod/**

Статический сайт на GitHub Pages: один `index.html` с вшитыми данными, рисуется в браузере.
Обновляется пересборкой из `build\` и `git push` (данные — из базы знаний, Битрикс24 и бота).

**Как устроено и как обновить — см. [КАК-РАБОТАЕТ.md](./КАК-РАБОТАЕТ.md).**

> Внутренний ресурс ООО «СКС Ломбард». Ссылка публичная (по URL), из поиска скрыта (`noindex`).
> В паблик-репозиторий уходит только `index.html`; файлы с ПДн и вебхук — в `.gitignore`.
