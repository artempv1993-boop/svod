# ─────────────────────────────────────────────────────────────────────────────
# ПУБЛИЧНЫЙ репозиторий. Здесь запрещено всё, кроме собранных обезличенных страниц.
#
# Правило: игнорируем ВСЁ, разрешаем поимённо. Так новый файл с персональными
# данными не попадёт в публикацию по невнимательности — его придётся разрешить
# явной строкой ниже.
#
# Страницы собирает knowledge-base/_scripts/svod-anon-build.py из приватных
# исходников в базе знаний. Ни исходники, ни конвейер, ни промежуточные файлы
# (payload.json, roster.json, report.md, adelina.html) сюда не кладутся —
# они живут в приватном репозитории базы знаний.
# ─────────────────────────────────────────────────────────────────────────────
*

!.gitignore
!README.md
!index.html
!ii.html
!.nojekyll
